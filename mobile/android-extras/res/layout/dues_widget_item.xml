<?xml version="1.0" encoding="utf-8"?>
<!-- Two levels on purpose: a ListView gives its items AbsListView.LayoutParams,
     which carry no margins, so the gap between cards has to be padding on an
     outer wrapper. The card surface sits on the inner layout. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/dues_row_bg"
        android:paddingStart="10dp"
        android:paddingEnd="12dp"
        android:paddingTop="11dp"
        android:paddingBottom="11dp"
        android:gravity="center_vertical">

        <!-- Direction, read before any text: green bar for money coming in,
             red for money going out. Set per row by DuesFactory. -->
        <ImageView
            android:id="@+id/item_accent"
            android:layout_width="3dp"
            android:layout_height="30dp"
            android:layout_marginEnd="10dp"
            android:contentDescription="@null"
            android:background="@drawable/dues_accent_out" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/item_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="14sp"
                android:textColor="#E8EBF4" />

            <TextView
                android:id="@+id/item_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:maxLines="1"
                android:textSize="11sp"
                android:textColor="#8A93A8" />
        </LinearLayout>

        <TextView
            android:id="@+id/item_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:background="@drawable/dues_pill_out"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:textStyle="bold"
            android:textSize="13sp"
            android:textColor="#F87171" />
    </LinearLayout>
</LinearLayout>
