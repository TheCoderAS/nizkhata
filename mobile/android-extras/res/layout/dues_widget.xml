<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/dues_widget_bg"
    android:padding="14dp">

    <!-- Header: the section owns the line, and the two totals sit in tinted
         pills rather than as more bold text at the same size as everything
         else. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="10dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Dues"
            android:textStyle="bold"
            android:textSize="16sp"
            android:letterSpacing="-0.02"
            android:textColor="#E8EBF4" />

        <TextView
            android:id="@+id/widget_receivable"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/dues_pill_in"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="In —"
            android:textSize="11sp"
            android:textStyle="bold"
            android:layout_marginEnd="6dp"
            android:textColor="#34D399" />

        <TextView
            android:id="@+id/widget_payable"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/dues_pill_out"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="Out —"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="#F87171" />
    </LinearLayout>

    <!-- No dividers: every row carries its own surface and spacing. -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:clipToPadding="false"
        android:scrollbars="none" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:gravity="center"
        android:text="Nothing due. All clear."
        android:textSize="12sp"
        android:textColor="#8A93A8" />
</LinearLayout>
