<?xml version="1.0" encoding="utf-8"?>
<!-- Each due is its own surface. Rows used to be separated by hairline rules,
     which is what made the list read like a printed table. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#171A21" />
    <corners android:radius="16dp" />
    <stroke android:width="1dp" android:color="#242932" />
</shape>
